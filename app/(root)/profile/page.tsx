"use client"
import React from 'react'

const ProfilePage = () => {
  return (
    <div>ProfilePage
      <button onClick={() => alert('tp')} >TAP TAP</button>
    </div>
  )
}

export default ProfilePage